# CLAUDE.md — 五行バランスチェック

作業前に必ず `PROJECT.md` を読むこと(目的・機能・データ設計・デザイン思想・残タスクの正はそこ)。

## このプロジェクトの絶対ルール

1. 「診断」表記禁止 → 「バランスチェック」。免責・受診勧奨を消さない
2. 症状チェックにLLMを使わない(ルールベース維持。監修可能性のため)
3. `data/mapping.json` を変えたら `docs/対応表ドラフト_v0.1.md` も同時更新(1対1整合)
4. JS/CSS変更時はキャッシュバスター `?v=0XX` を上げる
5. デプロイはまとめて最小回数(Netlifyクレジット制・過去に枯渇事故)
6. `server/village-worker/.dev.vars` はコミット禁止(gitignore済み)
7. 参考素材3フォルダ(五行キャラ参考/人体参考イラスト/精霊シート)はgit管理外。削除・移動しない
8. オーナーは非エンジニア。説明は日本語で、推奨案1つ+YES/NO形式

## 作業後

- 変更内容をPROJECT.mdに反映(更新日も)
- コミットして(可能なら)GitHubへpush — ChatGPT側もこのリポジトリを見て併走している
